"use client";
import { Toggle } from "keep-react-demo";

const SwitchComponent = () => {
  return <Toggle bgColor="primary" label="Toggle" size="md" withIcon={true} />;
};

export default SwitchComponent;
